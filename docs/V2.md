# Cravia — V2 Backlog

Tasks deferred to v2. Add new items at the bottom of the list.

## Tasks

- [ ] Receipt-verified reviews: let users upload an order receipt, verify it via OCR/AI, and display a "Verified" badge on the review - add backend for verification, frontend done.
- [ ] Restaurant cover image from UGC: auto-select the highest-rated dish photo as the restaurant cover image once enough reviews accumulate
- [ ] Restaurant owner profiles: allow restaurant owners to claim their profile and upload official cover photos and menu images
- [ ] Once restaurant is claimed, add option of scanner for user to open it and check the complete menu with dish reviews and recommeneded dishes along with it
- [ ] Add option of scanner for feedback after dining in a restaurant, that lets user review dishes they ate on the platform.
- [ ] Show recommeneded dishes to user based on reviews
- [ ] Migrate ExploreSearchResults local state to TanStack Query (['explore', query]) so search results are cached between navigations instead of re-fetched on every return visit.
