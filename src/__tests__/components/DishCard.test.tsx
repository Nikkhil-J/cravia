/**
 * DishCard component tests.
 *
 * @testing-library/react is NOT installed. Tests are it.todo.
 * Install @testing-library/react + jsdom to enable.
 */

import { describe, it } from 'vitest'

describe('DishCard', () => {
  it.todo('renders the dish name')

  it.todo('shows "No reviews yet" when reviewCount is 0')

  it.todo('shows rating bars when reviewCount is > 0')

  it.todo('shows "Limited data" badge when reviewCount < 3')

  it.todo('shows "Reliable" badge when reviewCount >= 10')

  it.todo('renders coverImage when present')

  it.todo('renders placeholder emoji when coverImage is null')

  it.todo('renders dietary indicator for veg dishes')

  it.todo('renders dietary indicator for non-veg dishes')

  it.todo('links to the correct /dish/[id] route')
})
